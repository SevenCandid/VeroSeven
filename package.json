{
  "name": "veroseven-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix veroseven-hq && npm run build --prefix veroseven-hq && rm -rf dist && cp -r veroseven-nexus dist && mkdir -p dist/admin && cp -r veroseven-hq/dist/* dist/admin/"
  }
}
